name: Triggers

run-name: Second Challenge

on:
  issue_commet:
    types: [ created, deleted ]
  push:
    branches:
    - main
  pull_request:
    types:
    - ready_for_review

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: 21
    - name: hello
      run: node index.js
